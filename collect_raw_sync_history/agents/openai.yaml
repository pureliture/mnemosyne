interface:
  display_name: "Raw 동기화 이력"
  short_description: "날짜 구간의 raw 동기화 작업을 조회합니다"
  default_prompt: "Use $collect-raw-sync-history to list work synced to the active raw repository in a date range."
policy:
  allow_implicit_invocation: true
